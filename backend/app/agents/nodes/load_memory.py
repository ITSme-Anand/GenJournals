from app.agents.state import AgentState

def loadMemoryNode(state:AgentState):
    #we have to load long term memory from persistence layer
    return {"longTermMemory": "user's is in love with Biriyani"}